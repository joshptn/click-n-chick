// The marketing menu now comes from the seeded catalogue via /api/foods -
// see MenuSection. MENU_ITEMS used to live here as a hardcoded placeholder and
// has been removed so the landing page and the customer home page cannot show
// two different menus.
export const NAV_LINKS = [
  ['menu', 'Menu'],
  ['reviews', 'Reviews'],
  ['contact', 'Contact'],
];

export default NAV_LINKS;
